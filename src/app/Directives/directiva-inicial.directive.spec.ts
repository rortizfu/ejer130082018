import { DirectivaInicialDirective } from './directiva-inicial.directive';

describe('DirectivaInicialDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaInicialDirective();
    expect(directive).toBeTruthy();
  });
});
